# Configuración SIFEN
# Copia este archivo a .env y completa con tus valores reales
# IMPORTANTE: NO commits .env al repo (ya está en .gitignore)

# Ambiente SIFEN: 'test' o 'prod'
SIFEN_ENV=test

# Ruta al certificado P12 (requerido para operaciones SIFEN)
# Ejemplo: /path/to/cert.p12
SIFEN_P12_PATH=

# Password del certificado P12 (requerido en modo web, opcional en CLI)
# En CLI, si no está, se pedirá interactivamente
SIFEN_P12_PASSWORD=

# Alternativa: usar nombres de variables legacy (también soportados)
# SIFEN_P12_PASS=

# Saltar validación de RUC antes de enviar DE (útil para testing)
# 0 = validar RUC, 1 = saltar validación
SIFEN_SKIP_RUC_GATE=0

# RUC para consultas de prueba (usado por scripts/sifen_smoke_consulta_ruc.sh)
# Ejemplo: 45547378
SIFEN_RUC_CONS=

# Timeout HTTP para requests SIFEN (en segundos, default: 30)
# SIFEN_HTTP_TIMEOUT=30

# Configuración QR oficial (gCamFuFD/dCarQR)
# requerido para generar QR válido según especificación SIFEN
SIFEN_QR_IDCSC=0001
SIFEN_QR_CSC=
SIFEN_QR_BASE_URL=https://ekuatia.set.gov.py/consultas-test/qr
